/**
 * include header to include glfw and glew. This is needed to easily resolve
 * include dependecies between the two, that require glew to be included before
 * glfw
 */

#include <GL/glew.h>
#include <GL/glfw3.h>
